name: StatisticsAPI CI

on:
  push:
    paths:
      - 'application/lbsite/StatisticsAPI/**'
      - '.github/workflows/statistics-api-ci.yml'
  pull_request:
    paths:
      - 'application/lbsite/StatisticsAPI/**'
      - '.github/workflows/statistics-api-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: application/lbsite/StatisticsAPI

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Run unit tests
        run: mvn -B test

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: statistics-api-test-reports
          path: |
            application/lbsite/StatisticsAPI/target/surefire-reports/**
            application/lbsite/StatisticsAPI/target/failsafe-reports/**
          if-no-files-found: warn

  build:
    runs-on: ubuntu-latest
    needs: test
    defaults:
      run:
        working-directory: application/lbsite/StatisticsAPI

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Build with Maven (package)
        run: mvn -B -DskipTests package

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: statistics-api-jar
          path: application/lbsite/StatisticsAPI/target/*.jar
          if-no-files-found: error
